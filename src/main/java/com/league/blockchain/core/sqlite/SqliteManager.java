package com.league.blockchain.core.sqlite;

import com.league.blockchain.core.event.ClientRequestEvent;
import org.springframework.context.ApplicationEvent;

/**
 * 客户端对外发请求时会触发该Event
 */
public class SqliteManager extends ApplicationEvent {
    public ClientRequestEvent(BloP)
}
